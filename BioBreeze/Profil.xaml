<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="BioBreeze.Profil"
             Title="Profil">
    <ScrollView>
        <StackLayout>
            <Label 
                Text="Premii obținute:"
                VerticalOptions="Center" 
                HorizontalOptions="Center" />
            <StackLayout>
                <!-- Badge for 1 test -->
                <Image x:Name="Badge1" WidthRequest="320" HeightRequest="320"/>

                <!-- Badge for 5 tests -->
                <Image x:Name="Badge5" WidthRequest="320" HeightRequest="320"/>

                <!-- Badge for 10 tests -->
                <Image x:Name="Badge10" WidthRequest="320" HeightRequest="320"/>

                <!-- Badge for 25 tests -->
                <Image x:Name="Badge25" WidthRequest="320" HeightRequest="320"/>

                <!-- Badge for 50 tests -->
                <Image x:Name="Badge50" WidthRequest="320" HeightRequest="320"/>
            </StackLayout>
        </StackLayout>
    </ScrollView>
</ContentPage>
